import React from "react";
import { Input } from "semantic-ui-react";

const SearchUsers = () => (
  <Input icon="users" iconPosition="left" placeholder="Kullanıcı ara..." />
);

export default SearchUsers;
